# Server configuration
HOST=0.0.0.0
PORT=8000

# CORS
ALLOWED_ORIGINS=http://localhost:3000

# AI Model
MODEL_NAME=Your-model-name-here

# Session
SESSION_TIMEOUT_MINUTES=15

# Database
DATABASE_URL=postgresql://brukernavn:passord@host:port/databasenavn
AZURE_CONNECTION_STRING=Your-Azure-Blob-Storage-connection-string-here
BLOB_CONTAINER_NAME=Your-blob-container-name-here

# MCP
MCP_TRANSPORT=sse
MCP_PORT=9000

# TOOL CONFIGS
BUFFER_DISTANCE_MIN_METERS=10
BUFFER_DISTANCE_MAX_METERS=50000
BUFFER_RESULT_LIMIT=200

# GitHub Models embeddings API (for semantic search)
GITHUB_MODELS_TOKEN=ghp_your_fine_grained_token_here

# Azure OpenAI embeddings (higher rate limits)
# If both Azure OpenAI and GitHub Models are configured, Azure OpenAI takes priority.
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com
AZURE_OPENAI_API_KEY=your-azure-openai-api-key-here
AZURE_OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-3-large
AZURE_OPENAI_API_VERSION=2024-10-21
# Must match DB schema's vector(N) column size. text-embedding-3-large natively outputs 3072 dims.
AZURE_OPENAI_EMBEDDING_DIMENSIONS=1536

# Indexing
INDEXING_ENABLED=true

# DEMO
DEMO_MODE=true or false

# SQL query allowlist — comma-separated PostgreSQL schemas the agent may SELECT from
SQL_ALLOWED_SCHEMAS=schema1,schema2
